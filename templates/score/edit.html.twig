{% extends 'base.html.twig' %}

{% block title %}Edit Score{% endblock %}

{% block body %}
    <div class="container">

    <h1 class="d-flex justify-content-center">Edit Score with {{ user.name }}</h1>

    {{ include('score/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_score_index', {'user': user.id}) }}">back to list</a>

    {{ include('score/_delete_form.html.twig') }}
    </div>
{% endblock %}
